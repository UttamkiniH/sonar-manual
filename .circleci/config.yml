version: 2.1



jobs:

  sonarqube:
    docker:
      - image: circleci/node:14
    steps:
      - checkout
      - run: npm install
      - run: npm run test -- --coverage
      - run:
          name: Analyze with SonarQube
          command: |
            curl --fail -X POST \
              -H 'Content-Type: application/x-www-form-urlencoded' \
              -d "projectKey=sqp_65c7d89595ade54f5c27bb0270f86b1f77a36487" \
              -d "projectName=sonar-manual" \
              -d "sonar.sources=." \
              -d "sonar.host.url=https://sonarqube.alpha.blr.gluestack.app"
             

workflows:
  build-and-test:
    jobs:
      - sonarqube

env:
  SONARQUBE_TOKEN: sqp_65c7d89595ade54f5c27bb0270f86b1f77a36487